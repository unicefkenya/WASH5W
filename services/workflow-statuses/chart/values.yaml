# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: workflowStatuses
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/workflowStatuses
        backend:
          service:
            name: workflowStatuses
            port:
              number: 80

  # Service
  service:
    name: workflowStatuses
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: workflowStatuses

  # Application / Application Replicas
  application:
    name: workflowStatuses
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/workflowStatuses
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

