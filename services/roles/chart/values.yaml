# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: roles
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/roles
        backend:
          service:
            name: roles
            port:
              number: 80

  # Service
  service:
    name: roles
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: roles

  # Application / Application Replicas
  application:
    name: roles
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/roles
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment: 
      postgres:
        host: bitnami-postgres-postgresql        
    replicas: 1

