# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: operators
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/operators
        backend:
          service:
            name: operators
            port:
              number: 80

  # Service
  service:
    name: operators
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: operators

  # Application / Application Replicas
  application:
    name: operators
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/operators
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

