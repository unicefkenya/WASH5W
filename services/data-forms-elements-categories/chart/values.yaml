# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: dataFormsElementsCategories
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/dataFormsElementsCategories
        backend:
          service:
            name: dataFormsElementsCategories
            port:
              number: 80

  # Service
  service:
    name: dataFormsElementsCategories
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: dataFormsElementsCategories

  # Application / Application Replicas
  application:
    name: dataFormsElementsCategories
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/dataFormsElementsCategories
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

