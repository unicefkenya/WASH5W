# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: workflows
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/workflows
        backend:
          service:
            name: workflows
            port:
              number: 80

  # Service
  service:
    name: workflows
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: workflows

  # Application / Application Replicas
  application:
    name: workflows
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/workflows
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

