# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: levels
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/levels
        backend:
          service:
            name: levels
            port:
              number: 80

  # Service
  service:
    name: levels
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: levels

  # Application / Application Replicas
  application:
    name: levels
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/levels
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment: 
      postgres:
        host: bitnami-postgres-postgresql        
    replicas: 1

