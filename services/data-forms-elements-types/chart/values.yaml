# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: dataFormsElementsTypes
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/dataFormsElementsTypes
        backend:
          service:
            name: dataFormsElementsTypes
            port:
              number: 80

  # Service
  service:
    name: dataFormsElementsTypes
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: dataFormsElementsTypes

  # Application / Application Replicas
  application:
    name: dataFormsElementsTypes
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/dataFormsElementsTypes
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

