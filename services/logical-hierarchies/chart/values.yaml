# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: logicalHierarchies
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/logicalHierarchies
        backend:
          service:
            name: logicalHierarchies
            port:
              number: 80

  # Service
  service:
    name: logicalHierarchies
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: logicalHierarchies

  # Application / Application Replicas
  application:
    name: logicalHierarchies
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/logicalHierarchies
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

