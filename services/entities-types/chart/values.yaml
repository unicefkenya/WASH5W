# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: entitiesTypes
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/entitiesTypes
        backend:
          service:
            name: entitiesTypes
            port:
              number: 80

  # Service
  service:
    name: entitiesTypes
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: entitiesTypes

  # Application / Application Replicas
  application:
    name: entitiesTypes
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/entitiesTypes
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

