# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: systemsRoles
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/systemsRoles
        backend:
          service:
            name: systemsRoles
            port:
              number: 80

  # Service
  service:
    name: systemsRoles
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: systemsRoles

  # Application / Application Replicas
  application:
    name: systemsRoles
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/systemsRoles
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

