# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: options
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/options
        backend:
          service:
            name: options
            port:
              number: 80

  # Service
  service:
    name: options
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: options

  # Application / Application Replicas
  application:
    name: options
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/options
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

