# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: attributes
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/attributes
        backend:
          service:
            name: attributes
            port:
              number: 80

  # Service
  service:
    name: attributes
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: attributes

  # Application / Application Replicas
  application:
    name: attributes
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/attributes
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment: 
      postgres:
        host: bitnami-postgres-postgresql        
    replicas: 1

