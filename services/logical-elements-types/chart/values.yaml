# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: logicalElementsTypes
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/logicalElementsTypes
        backend:
          service:
            name: logicalElementsTypes
            port:
              number: 80

  # Service
  service:
    name: logicalElementsTypes
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: logicalElementsTypes

  # Application / Application Replicas
  application:
    name: logicalElementsTypes
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/logicalElementsTypes
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

