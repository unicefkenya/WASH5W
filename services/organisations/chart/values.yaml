# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: organisations
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/organisations
        backend:
          service:
            name: organisations
            port:
              number: 80

  # Service
  service:
    name: organisations
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: organisations

  # Application / Application Replicas
  application:
    name: organisations
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/organisations
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

