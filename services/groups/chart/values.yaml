# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: groups
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/groups
        backend:
          service:
            name: groups
            port:
              number: 80

  # Service
  service:
    name: groups
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: groups

  # Application / Application Replicas
  application:
    name: groups
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/groups
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment: 
      postgres:
        host: bitnami-postgres-postgresql        
    replicas: 1

