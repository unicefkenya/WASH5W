# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: systemsModulesPermissions
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/systemsModulesPermissions
        backend:
          service:
            name: systemsModulesPermissions
            port:
              number: 80

  # Service
  service:
    name: systemsModulesPermissions
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: systemsModulesPermissions

  # Application / Application Replicas
  application:
    name: systemsModulesPermissions
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/systemsModulesPermissions
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

