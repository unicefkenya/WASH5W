# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: systemsUsers
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/systemsUsers
        backend:
          service:
            name: systemsUsers
            port:
              number: 80

  # Service
  service:
    name: systemsUsers
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: systemsUsers

  # Application / Application Replicas
  application:
    name: systemsUsers
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/systemsUsers
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

