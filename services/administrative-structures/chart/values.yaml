# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: administrativeStructures
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/administrativeStructures
        backend:
          service:
            name: administrativeStructures
            port:
              number: 80

  # Service
  service:
    name: administrativeStructures
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: administrativeStructures

  # Application / Application Replicas
  application:
    name: administrativeStructures
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/administrativeStructures
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

