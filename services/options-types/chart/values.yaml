# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: optionsTypes
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/optionsTypes
        backend:
          service:
            name: optionsTypes
            port:
              number: 80

  # Service
  service:
    name: optionsTypes
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: optionsTypes

  # Application / Application Replicas
  application:
    name: optionsTypes
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/optionsTypes
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

