# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: dissagregations
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/dissagregations
        backend:
          service:
            name: dissagregations
            port:
              number: 80

  # Service
  service:
    name: dissagregations
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: dissagregations

  # Application / Application Replicas
  application:
    name: dissagregations
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/dissagregations
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

