# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: contexts
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/contexts
        backend:
          service:
            name: contexts
            port:
              number: 80

  # Service
  service:
    name: contexts
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: contexts

  # Application / Application Replicas
  application:
    name: contexts
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/contexts
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

