# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: administrativeUnitsTypes
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/administrativeUnitsTypes
        backend:
          service:
            name: administrativeUnitsTypes
            port:
              number: 80

  # Service
  service:
    name: administrativeUnitsTypes
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: administrativeUnitsTypes

  # Application / Application Replicas
  application:
    name: administrativeUnitsTypes
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/administrativeUnitsTypes
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

