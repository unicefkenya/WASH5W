# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: statuses
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/statuses
        backend:
          service:
            name: statuses
            port:
              number: 80

  # Service
  service:
    name: statuses
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: statuses

  # Application / Application Replicas
  application:
    name: statuses
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/statuses
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment: 
      postgres:
        host: bitnami-postgres-postgresql        
    replicas: 1

