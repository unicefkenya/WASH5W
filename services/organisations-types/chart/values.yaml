# Default values for the Microservice's Helm Chart.
# This is a YAML-formatted file.

# MICROSERVICE
# ----------------------------------------------------------------------------------------
microservice:

  # Ingress
  ingress:
    name: organisationsTypes
    class: traefik
    rules:
      host: cloud.miles.co.ke
      http:
        path: /api/v1/organisationsTypes
        backend:
          service:
            name: organisationsTypes
            port:
              number: 80

  # Service
  service:
    name: organisationsTypes
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: organisationsTypes

  # Application / Application Replicas
  application:
    name: organisationsTypes
    version: 0.0.1
    image:
      id: cloud.miles.co.ke:5043/organisationsTypes
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment:
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

