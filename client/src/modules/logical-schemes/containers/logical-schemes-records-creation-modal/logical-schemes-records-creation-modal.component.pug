sb-modal(shadow='false')
  ng-container(title='')
    | {{title}}
  ng-container(body='')
    sb-logical-schemes-records-creation((succeeded)='onSucceeded()', (failed)='onFailed($event)')
    ngcontainer([ngswitch]='(status$ | async)')
      // Saving Overlay
      sb-message-overlay(*ngswitchcase="'saving'", activity='creating', status='saving')
      // Saving Suceeded Overlay
      sb-message-overlay(*ngswitchcase="'succeeded'", activity='creating', status='succeeded')
      // Saving Failed Overlay
      sb-message-overlay(*ngswitchcase="'failed'", activity='creating', status='failed')
      // Saving Re-Trial Delay Overlay
      sb-countdown-overlay(*ngswitchcase="'retrying'", crosshead='Retrying in', (completed)='onSave()')
      // Done Overlay
      sb-message-overlay(*ngswitchcase="'done'", activity='creating', status='done')
  ng-container(footer='')
    ngcontainer([ngswitch]='(status$ | async)')
      // Ready State Buttons
      sb-button(*ngswitchcase="'ready'", action='save', entity='LogicalScheme', (save)='onSave()')
      // Invalid  State Buttons
      sb-button(*ngswitchcase="'invalid'", action='save', entity='LogicalScheme', (save)='onSave()')
      // Saving State Buttons
      sb-button(*ngswitchcase="'saving'", action='save', entity='LogicalScheme', disabled='true')
      // Succeeded State Buttons
      sb-button(*ngswitchcase="'succeeded'", action='yes', entity='LogicalScheme', guarded='false', (yes)='onContinue()')
      sb-button(*ngswitchcase="'succeeded'", action='no', entity='LogicalScheme', guarded='false', (no)='onQuit()')
      // Failed State Buttons
      sb-button(*ngswitchcase="'failed'", action='yes', entity='LogicalScheme', (yes)='onRetry()')
      sb-button(*ngswitchcase="'failed'", action='no', entity='LogicalScheme', guarded='false', (no)='onQuit()')
      // Retrying State Buttons
      sb-button(*ngswitchcase="'retrying'", action='yes', entity='LogicalScheme', disabled='true')
      sb-button(*ngswitchcase="'retrying'", action='no', entity='LogicalScheme', disabled='true')
      // Done Buttons
      sb-button(*ngswitchcase="'done'", action='done', entity='LogicalScheme', guarded='false', (done)='onDone()')
